@foreach (var line in lines)
{
    <span class="terminal output" >@((MarkupString)line)</span>
}

@code {
    public List<string> lines  = new List<string>();

    public void AddLine(string line)
    {
        lines.Add(line);
        StateHasChanged();
    }
}
